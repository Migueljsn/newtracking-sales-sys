"use client";

// @deprecated — replaced by PipelineStageSelector
// This component is kept for backward compatibility but no longer used in the UI.

export function MarkRegisteredButton({ leadId: _leadId }: { leadId: string }) {
  return null;
}
